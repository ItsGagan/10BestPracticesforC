#include <assert.h>

int main() {
    // Declare an integer variable
    int x = 10;

    // Use the assert macro to check that the value of x is greater than 0
    assert(x > 0);

    // Use the assert macro to check that the value of x is less than 20
    assert(x < 20);

    return 0;
}
